# CD-Remote
I wanted to replicate a tiny fraction of the power of EMACS TRAMP, and wanted to write a little utility to cd to a remote directory.

## Usage
Put `cdr` somewhere in your path.

```
cdr <ip> <directory>
```

